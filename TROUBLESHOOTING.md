# TROUBLESHOOTING · Yxi_Entertainment

> 只增不删。每条 = 症状 → 根因 → 修法/怎么避开。
> 属于 `Yxi` / `logto_yxi` 自己的坑记在它们各自的表里，这里只记我这个位置上踩到的。

## 1. 改了一半、没编译过的文件被 cc-Yxi 的提交一起 push 了

**症状**（2026-09-05）：我刚用脚本改完 `Account.kt` / `SettingsScreen.kt` / `En.kt`、新建 `Badges.kt`，
一分钟后 `git status` 里只剩 `MailScreen.kt` —— 另外四个文件不见了。`git show --stat HEAD` 一看，
它们全在 cc-Yxi 08:23 那个「临时会话」提交（1694114）里，已经 push。

**根因**：同一个工作树里两个 agent 同时干活，cc-Yxi 提交用的是 `git add -A`，工作树里凡是有改动的都被卷走。
他提交时我还没 build，所以那是一版**没验证过**的代码进了主线。

**修法 / 避开**：
- 改 `Yxi/` 之前先 `yxi-hub say cc-Yxi` 说清碰哪几个文件；改完**立刻** build 验证，别让半成品在树里过夜。
- 提交按路径 add（`git add <文件>`），也请 cc-Yxi 在我改的期间别 `-A`。
- 发现被卷走时：**别慌着 revert** —— 先看内容是否自洽、能否编过；能编过就正常接着做，
  只需通知 cc-Yxi「HEAD 里有我没验证的东西，E2E 完之前别从 HEAD 发版」。

## 2. 别在 cc-Yxi 跑模拟器 E2E 时编译

**症状**：`uptime` 负载 28（16 核），模拟器 430% CPU。这时候 gradle 一编，他那边 uiautomator 会静默失效
（`Yxi_pilot/TROUBLESHOOTING.md` 记过：高负载下 dump 返回空、点按不生效、看起来像 App 坏了）。

**避开**：编译前 `hub peek cc-Yxi` + `uptime`；他说「模拟器归你」再动。轮到我 E2E 时同理，反过来提醒他。

## 3. 跑 `connectedDebugAndroidTest` 会把模拟器上的 App 数据清掉（登录态、主机配置、通知权限全没）

**症状**（2026-09-05）：跑完 TicketsTest 再起 App，先弹「Allow Yxi to send you notifications?」，接着是登录门禁页，看板「还没有主机」。

**根因**：仪器测试的安装流程会卸载重装（cc-Yxi 也记过「每次跑测试 Gradle 都重装 App，密钥就换了一把」）——
Keystore 里的加密钥换了，存着的令牌解不开，等于没登录。

**修法 / 避开**：UI E2E **先跑完再跑仪器测试**，或者测完重新登一次：点「登录 / 注册」进浏览器，浏览器里若还留着 Logto 会话，
OIDC 一跳就回来，不用输密码；否则测试号密码在 `/root/.secrets/yxi-app-test.txt`。主机 `dev=10.0.2.2` 要重加（工单 / 邮件页不需要主机）。

## 4. uiautomator 点按前先收软键盘，否则点在键盘上

**症状**：输完文字点「发送」，什么都没发生，输入框末尾多了一个 `o`。

**根因**：`uiautomator dump` 给的是 App 窗口的节点坐标，**不知道 IME 窗口盖在上面**。「发送」按钮的坐标此刻正好是键盘的 o 键。
`keyevent 111`（ESC）收不掉这个键盘。

**避开**：点按前查 `adb shell dumpsys input_method | grep mInputShown=true`，是 true 就先 `keyevent 4`（BACK，只收键盘不退页），再 dump 一次拿新坐标。
点按的文字匹配要**精确优先**：「Allow」会先匹到「Allow Yxi to…」那行标题（`e2e/ui.py` 已改）。

## 5. 往 Mac mini 传大文件时隧道断掉

**症状**（2026-09-05 14:17）：`scp` 一个 98MB 的 debug APK 到 `mac:/tmp`，2 分钟后「Connection closed by 127.0.0.1 port 2223」，
同时在跑的 `emu.sh start` 也断；之后十几分钟 `ssh mac` 一直 banner exchange 超时，14:31 自己回来了。

**根因**：没有实证。隧道是 Mac 那头一次性 `ssh -f -N` 起的，大流量 + 同时另一条 ssh 跑模拟器启动，掉线后要等那头重连。

**避开**：传包用 `rsync -e ssh --partial --inplace`（断了能续，第二次 18 秒传完）；传大文件时别同时跑别的 ssh 重活；
`ssh mac` 超时先挂个每分钟一探的后台循环，别一遍遍手点。

## 6. 对着队友的「接口文件」写代码，编不过还挡了别人的 E2E

**症状**（2026-09-06）：按 `Yxi_pilot/design/yunxi-api.md` 写云曦页，调了 `WeatherApi.glyph(code)`；文件里有这个函数，**落地的 Weather.kt 里没有**（实现成了 `kind(code)`）。
我写完没立刻编，工作树整个模块编不过，cc-Yxi 的 E2E 跑到 build 那一步被挡了几分钟。

**根因**：接口文件是「约定」，实现落地时会变；共享工作树里一个人的半成品就是所有人的编译错误。

**避开**：① 队友文件落地后**以源码为准**（`grep -n 'fun ' xxx.kt`），别只看接口文档；② 新文件写完**立刻** `run.sh build`，编不过就先修或先把文件挪出 `src/`，别让红的过夜；③ 编过之前别离开去做别的。

## 7. 模拟器前台突然变成别的 App（omggrow 任务台），不是别人抢，是任务栈

**症状**（2026-09-06，两次）：E2E 中途 dump 出来全是「omggrow 任务台」的节点，Yxi 不见了。cc-Yxi 之前也撞过一次。

**根因**：`am instrument` / 重装都会杀掉 Yxi 进程，系统把**最近任务栈里上一个任务**顶回前台 —— 那台 Mac 模拟器上别的组装过包、没卸。omggrow 那边查了 logcat 确实没有 START 记录。

**避开**：各组用完模拟器 `adb shell am force-stop <包>` 并卸掉测试包（omggrow 已改脚本）；自己的 E2E 每一段开头别假设前台还是自己的 App，先 `am start` 再点。

## 8. uiautomator dump 看不到云曦的气泡文字；6 秒的台词截图赶不上

**症状**：点她冒「嗯？」、进页说「都做完了」，截图上都有，`uiautomator dump` 的节点树里却没有这行字，`ui.py has/tap` 判「没找到」。

**根因**：不清楚为什么 dump 漏这个节点（Compose 的 AnimatedVisibility + Surface 组合下语义节点可能被合并/裁掉），**判气泡只能看截图**。另外经 ssh 的 dump/截图一趟四五秒，6 秒的台词多半错过 —— 已把台词停留改成 9 秒（顺带对人也更友好）。

**避开**：气泡类瞬时 UI 用截图验、别用文本树；进页后 2 秒内截图。

## 9. E2E 跑到一半界面「少了个入口」——不是代码坏了，是队友把你的包顶掉了

**症状**：同一个 APK，几分钟前「我的」页还有「个性化」入口、点得进去，重跑一遍就 `没找到「个性化」`，
dump 里那一行整个没了；应用没崩、logcat 无异常。

**根因**：模拟器是四个 agent 共用的，包名都是 `app.yxi`。队友从**自己的 worktree** 装了一版
（只含他自己的提交，没有我未提交的改动），`adb install -r` 直接覆盖，我的入口自然消失。
紧接着他起 `am instrument` 跑压测，又把进程重启一次，我正在点的那一轮全废。

**修法 / 怎么避开**：
- 上机第一件事：`adb shell dumpsys package app.yxi | grep lastUpdateTime` 跟自己刚建的 APK 时间比一眼，
  不对就重装自己的包再点（`bubbles.sh install`）。
- 中途结果反常，先查是不是被顶了：`adb logcat -d | grep -E 'TestRunner:|lastUpdateTime'`；
  `TestRunner: run started` 就是有人在跑 instrument，等 `run finished` 再继续（我等了 3 分钟）。
- 组里的规矩：**装包 / 起 instrument 之前先在 hub 吼一嗓子**，用完喊下一个人。别只看「屏幕空着」就当没人。

## 10. 气泡装饰用固定 dp，到了「装扮小样」那种小尺寸就压过本体、还被行裁掉

**症状**：聊天气泡在对话页和商店大预览里都对，但装扮列表里的小样（一个 40×32dp 的「Aa」气泡）：
「云曦想想」的探头图比气泡还大、把气泡盖住，左边还被列表行裁掉一块；像素气泡缩成一个「十字」。

**根因**：装饰（右下探出的头 64dp、尾巴两个圆圈、像素边三级台阶）都是**写死的 dp**，
不跟气泡大小走。气泡本体是内容撑出来的（文字 + padding），小样只有正常气泡的一半多点，
固定装饰就显得巨大；`BubbleBox` 给装饰留的外圈白边也是写死的，一起把 78×52dp 的小样盒子撑爆 → 被行 clip。
像素边同理：三级台阶按圆角算 12dp，小气泡高才 32dp，上下各吃 12dp，只剩 8dp 直边，看着就是十字。

**修法 / 怎么避开**：`BubbleBox(compact = true)` —— 小样把装饰和预留白边一起按 0.55 缩；
像素台阶再按**短边** `min(w,h)/9` 收一道。原则：**画在布局外面的装饰，尺寸必须跟着被装饰的东西缩**，
不然任何"小一号"的使用场景都会翻车（小样、密度小的屏、超短文字）。

## 11. 音游一局少一个判定，被服务端「判定数对不上」打回

**症状**：打完一局，结算页显示服务端的原话「判定数对不上：这张谱有 78 个判定，你报了 77 个」，
成绩不入账。数一下：完美 1 + 不错 0 + 漏了 76 = 77，确实少一个。

**根因**：长按音符**算两个判定**（头一个、尾巴到点时看手指还在不在）。
我在每条轨上放了个游标 `next[lane]`，避免每帧从头扫；`tapLane()` 找目标时顺手把游标写回去了：

```kotlin
while (i < lanes.size && lanes[i].judged != null) i++
live.next[lane] = i        // ← 这一行是错的
```

一个长按的**头**判过之后 `judged != null`，游标就被推过它，它的**尾巴**永远轮不到判 —— 一局少一个。

**修法 / 怎么避开**：游标只归 `judgeMisses()` 管（它知道尾巴判没判），`tapLane()` 只往前找目标、不写回。
更一般的教训：**一个对象有两个阶段（头/尾、开始/结束）时，别用"判过了"当"可以跳过了"**。
另外——这个 bug 是**服务端的计数校验**抓到的（客户端报的判定数必须等于谱面的判定数），
这种跨端交叉校验很值钱：客户端自己算的数跟服务端的谱面对不上就打回，比什么单元测试都实在。

## 12. 用 ssh 逐次点击测节奏游戏 = 测了个寂寞

**症状**：写 E2E 点了 32 下想打出连击，结果准度 1.3%，而且「怎么才点几下歌就放完了」。

**根因**：每次 `ssh mac 'emu.sh adb shell input tap'` 往返约 1 秒，32 次就是半分多钟；
曲子才 63 秒。节奏游戏的判定窗口是 ±80/160 毫秒，这个量级的抖动根本不可能命中。

**修法 / 怎么避开**：把循环**放进设备里**跑，一次 ssh 搞定：
`adb shell "sleep 6; for i in 1 2 3; do input tap 135 1500; …; done"`。
即便如此 `input tap` 每次也有一两百毫秒开销（它是个 Java 进程），**别指望脚本能打出高准度**。
音游 E2E 该验的是**流程和计数**（判定数加起来等于谱面判定数、服务端收不收、成绩回不回填），
手感和准度只能人拿真机试。

## 13. 音游的界面每帧都在重组（自己写出来的）

**症状**：游戏页看着正常，但 `now`（音频时钟，每帧都变）被写在组合里的判断中：
`if (now < 0f) Text(倒计时)`、`if (now - lastJudgeAt < 0.35f) Text(判定字)`。
读一次 = 每帧整页重组，60fps 就是每秒 60 次。

**根因**：Compose 里**在组合期读一个每帧都变的状态**，等于把整个可组合函数绑到帧率上。
Canvas 的 `drawBehind` / `Canvas{}` 里读同一个状态只会重跑**绘制**阶段，那是对的；
组合期读就全跑。

**修法 / 怎么避开**：
- 要跟着帧变的**视觉**（缩放、透明度）放进 `Modifier.graphicsLayer { }` 的 lambda —— 那是绘制阶段。
- 要跟着帧变的**结构**（显示/隐藏一段文字）改成独立状态，值真的变了才写：
  倒计时存整数（一秒才变一次），判定字判定时写、`LaunchedEffect` 延时 350ms 清掉。
- 一句话：**组合期只读"变得慢"的状态**。

## 14. 主题色 getter 是按**角色**定的，名字里的颜色不作数

**症状**：把判定线画成 `Copper`，以为会是暖铜色，实机上出来是**蓝的**，跟青色音符糊成一片，
命中的光也不像"打中了"。

**根因**：`Copper` 是**槽位名 = 主操作色**，不是字面的铜色。
深色皮肤里它确实是 `#FFB787`，**浅色皮肤里故意取成 Google 蓝 `#0B57D0`**（`ui/theme/Palette.kt`，
那一行旁边就写着「主操作 = Google 蓝」）。全 app 两百多处直接写 `Copper`，换风格时不用动那两百多处 ——
**这是设计，不是 bug，千万别去把浅色那行改成真铜色**，那等于把整个浅色主题的主色换掉。

**修法 / 怎么避开**：分清这块界面**跟不跟皮肤走**：
- 跟着皮肤走的地方（普通页面）：用 `Copper` / `Amber` 是对的，它们表达的是角色（主操作 / 需要你注意）。
- **不跟皮肤走的地方**（音游那块固定深色的舞台）：**一个主题色 getter 都不能用**，要什么颜色自己写死常量。
  同类的还有 `Muted`（文字次要色）—— 在固定深色底上它可能浅到看不见。

一句话记法：**主题色表达的是"这是什么角色"，不是"这是什么颜色"。**
